<section class="w-full min-h-screen h-full flex justify-center items-center relative z-30">
    <div class="w-4/5 max-w-60 h-4/5 max-h-80 relative rounded overflow-hidden">
        <form action="" class="grid absolute w-full min-h-full py-8 px-4 text-gray-800 gap-2 inset-0 m-auto z-40">
            <h1 class="text-4xl text-center">Ingreso</h1>
            <select name="metodo" id="metodo">
                <option value="promedio">Promedio</option>
                <option value="fifo">FIFO</option>
            </select>
            <input type="date" name="fecha_ingreso" id="fecha_ingreso">
            <div class="grid items-center justify-center text-center">
                <label for="cantidad">Cantidad</label>
                <input type="number" id="cantidad" min="0" name="cantidad">

                <label for="precio_unitario">Precio</label>
                <input type="number" id="precio_unitario" min="0" name="precio_unitario">
            </div>
            <button class="outline-1 outline-gray-700 cursor-pointer hover:bg-gray-700 hover:text-white duration-300 py-1" type="submit">Añadir</button>
        </form>
        <span class="absolute w-full min-h-full inset-0 m-auto z-30"></span>
    </div>
</section>